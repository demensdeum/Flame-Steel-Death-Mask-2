export class PhysicsControllerCollision {
    constructor(alice, bob, distance, direction) {
        this.alice = alice;
        this.bob = bob;
        this.distance = distance;
        this.direction = direction;
    }
}
;
